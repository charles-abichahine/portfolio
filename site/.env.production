# GoatCounter's /count endpoint for the deployed site (see src/analytics.js).
# Not a secret: it is in every visitor's network log. Left unset in development,
# where every call prints to the console instead of being sent.
VITE_GOATCOUNTER=https://charlesabichahine.goatcounter.com/count
